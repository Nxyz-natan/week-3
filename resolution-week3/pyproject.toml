[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week3_nxyz109"
version = "0.0.1"
dependencies = [ "fastapi[standard]" ]

[project.scripts]
todo-api = "resolution_week3_nxyz109.main:main"